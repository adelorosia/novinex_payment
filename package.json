{
  "name": "novinex_payments",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS='--no-deprecation' next dev -p 3011",
    "build": "next build",
    "start": "next start -p 3998",
    "setup": "npm i && npm run build && pm2 start --name mainsite npm -- start --update-env --time && pm2 save",
    "deploy": "git pull --no-rebase && npm i && npm run build && pm2 restart mainsite --update-env --time && pm2 save",
    "cp": "node cli/cp.mjs"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "next": "15.4.5"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
